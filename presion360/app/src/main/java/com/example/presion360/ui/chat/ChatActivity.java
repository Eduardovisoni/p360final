package com.example.presion360.ui.chat;

import androidx.appcompat.app.AppCompatActivity;
import android.os.Bundle;

/**
 * Esta actividad ya no está en uso.
 * La funcionalidad de Chat en vivo ha sido movida a ChatIAFragment.
 * El historial de chat se maneja en ChatHistoryActivity y ChatDetailActivity.
 */
public class ChatActivity extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // Contenido eliminado ya que la actividad no se usa y causaba errores de compilación.
        // Si esta actividad se vuelve a necesitar, su contenido debe ser restaurado o recreado.
    }
}
